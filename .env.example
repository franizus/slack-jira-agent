# Variables de entorno para Slack Jira Agent

# Token de Bot de Slack (requerido para la API de Slack)
SLACK_BOT_TOKEN="xoxb-SLACK_BOT_TOKEN"

# URI de conexión a MongoDB (requerido para la persistencia de memoria del agente)
MONGODB_URI="mongodb+srv://username:password@subdomain.mongodb.net/"

# Credenciales para el LLM (Google Cloud / Vertex AI)
# Para Vertex AI, usualmente se configura GOOGLE_APPLICATION_CREDENTIALS
# o se usan roles de servicio si se ejecuta en GCP.
# GOOGLE_APPLICATION_CREDENTIALS="/ruta/a/tu/archivo-de-credenciales.json"
GOOGLE_API_KEY="API_KEY_DE_GOOGLE_AQUI"

# Si usaras OpenAI directamente:
# OPENAI_API_KEY="sk-tu-api-key-de-openai-aqui"

# URL del agente de desarrollo
DEVELOPMENT_AGENT_URL="http://test.com/v1/test"
DEVELOPMENT_AGENT_API_KEY="XXXXXX"

# Credenciales para Jira
# Asegúrate de que el usuario tenga permisos adecuados para crear issues.
# Puedes generar un token de API en tu perfil de Jira.
# Más información: https://support.atlassian.com/atlassian-account/docs/manage-api-tokens-for-your-atlassian-account/
JIRA_DOMAIN="your-domain"
JIRA_EMAIL="example@your-domain.com"
JIRA_API_TOKEN="your-jira-api-token"